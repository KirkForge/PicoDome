{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "irondome-admission.serviceAccountName" . }}
  labels:
    {{- include "irondome-admission.labels" . | nindent 4 }}
{{- end }}