apiVersion: v1
kind: Service
metadata:
  name: {{ include "irondome-admission.fullname" . }}
  labels:
    {{- include "irondome-admission.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: https
  selector:
    {{- include "irondome-admission.selectorLabels" . | nindent 4 }}