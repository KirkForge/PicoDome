{{- if .Values.auth.createSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "irondome.fullname" . }}-tokens
  labels:
    {{- include "irondome.labels" . | nindent 4 }}
type: Opaque
stringData:
  api-tokens: {{ .Values.auth.tokens | default (printf "irondome-admin-%s" (randAlphaNum 32)) | quote }}
{{- end }}