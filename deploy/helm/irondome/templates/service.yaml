apiVersion: v1
kind: Service
metadata:
  name: {{ include "irondome.fullname" . }}
  labels:
    {{- include "irondome.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
    {{- if .Values.metrics.separatePort }}
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
    {{- end }}
  selector:
    {{- include "irondome.selectorLabels" . | nindent 4 }}